<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="test.uts.hotel">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_hotel"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_hotel"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".RegisterActivity"
            android:theme="@style/AppThemeNoBar"
            android:screenOrientation="portrait"/>

        <activity android:name=".DetailKamarActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".ReceiptActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".ListkamarActivity"
            android:screenOrientation="portrait"/>
    </application>

</manifest>